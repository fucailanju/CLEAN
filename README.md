# CLEAN

CLEAN provides a minimal pipeline to proxy latent homophily structure (e.g., via SBM block recovery) and re-estimate contagion effects in panel models.

## Install (development version)

```r
# install.packages("remotes")
remotes::install_github("fucailanju/CLEAN")
